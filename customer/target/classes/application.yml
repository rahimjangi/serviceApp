server:
    port: 8080
spring:
    application:
        name: customer
    datasource:
        username: postgres
        url: jdbc:postgresql://localhost:5432/customer
        password: password
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
           hibernate:
                format_sql: true